# Environment configuration
# Copy to .env and fill in values

NODE_ENV=development
PORT=3000

# PostgreSQL connection string (required for runtime, test without it degrades gracefully)
DATABASE_URL=postgresql://user:password@localhost:5432/actor_platform
